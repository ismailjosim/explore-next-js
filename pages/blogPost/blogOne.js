import React from 'react'

const blogOne = () => {
    return (
        <div>
            <h3>How to create a nested route</h3>
            <p>
                Create a new folder and into that folder create a file and add some text. then from browser type localhost/foldername/filename
            </p>
        </div>

    )
}

export default blogOne
